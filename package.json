{
  "name": "authentication-flow",
  "version": "0.0.0",
  "private": true,
  "description": "Root dev tooling for the authentication-flow monorepo (markdown linting).",
  "scripts": {
    "lint:md": "markdownlint-cli2",
    "lint:md:fix": "markdownlint-cli2 --fix"
  },
  "devDependencies": {
    "markdownlint-cli2": "0.22.1"
  }
}
